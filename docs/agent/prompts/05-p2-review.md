# Prompt 05 — P2: self-review against the assignment before submission

> **Role reminder**: you are Claude Code, the implementer and tester. This prompt comes from the planning session (architect / product owner / QA lead), which never writes code. Scope, acceptance criteria and architecture are fixed here; implement them, test them, report back. Put any disagreement under "Questions for the architect" in your report — do not act on it.

Read `docs/PLAN.md` §1. Act as a skeptical reviewer of this repository. Do not change code in this session; produce a report only.

## Part 1 — Adversarial re-audit of P1/P2 surfaces (evidence = command + output, as in 01b; ~20 min)
A. **Status lookup** `GET /leads/track/{code}`: unknown code → 404 with the same body shape and timing as a known one (3 runs each); response contains ONLY state/timestamps/events — grep the JSON for name, email, resume, id; 21st request in a minute → 429; code is not the lead id and cannot be guessed (length/entropy check); SQL injection in the code path → 404 not 500.
B. **Rate limiter**: 6th `POST /leads` → 429 with Retry-After; different IP (X-Forwarded-For) does NOT bypass unless trusted-proxy is configured — state what the code does; login limiter independent of submission limiter.
C. **Upload sniffing**: PDF header + EXE body → 415; DOCX (zip) with wrong extension → 415; a real 4.9 MB PDF → 201; `.doc` → 415 with a clear message; honeypot filled → 202 and no row, no email, no event.
D. **Headers**: curl -I on API and web — nosniff, X-Frame-Options DENY, Referrer-Policy, CSP present; no `Server` version leakage.
E. **State machine**: PENDING→QUALIFIED → 409 invalid_transition; REACHED_OUT→QUALIFIED → 200 + event row + prospect email captured (notify_prospect); QUALIFIED→anything → 409; repeat → 409 already_in_state; 10 parallel REACHED_OUT→QUALIFIED → exactly one 200.
F. **Audit table**: every state change has exactly one lead_events row; rejected transitions write none; events not exposed on the public track endpoint beyond to_state/at.
G. **Email escaping**: `<img src=x onerror=…>` first name → escaped in HTML, raw in text; CRLF in email address → 422.
H. **Secrets/PII regression**: re-run 01b checks A1–A3 and B5–B6 on the full tree; `git log --all --name-only | grep -i '\.md$'` still empty; grep the Playwright fixtures and e2e output for personal data.
I. **Playwright**: run it twice; paste both results.
J. **CI**: `act` if available, else re-run the CI job steps locally including the Postgres job against a local Postgres if Docker exists — otherwise state plainly it is unverified locally and will be verified on the first push.

K. **Vocabulary**: also grep all .md in the working tree (excluding docs/private) for `staff|rubric|interview|reviewer` — any hit is a FINDING; docs describe the system and its trade-offs, never the evaluation. Then spec vocabulary: grep web/ and api/ for `immigration|visa|assessment|H-1B|O-1` — any hit in product copy, API text or email templates is a FINDING: revert to the assignment's vocabulary (lead intake, prospective client, attorney). Landing page: title "Lead intake", subtitle "Prospective clients submit their details; an attorney reviews and reaches out.", buttons "Apply" / "Attorney sign in". Domain inference is allowed ONLY in DESIGN.md, labelled as an assumption.

L. **Honeypot vs browser autofill — BLOCKER found in manual testing.** Chrome address-autofill fills the hidden field named `website` (it ignores `autocomplete="off"`), so a real applicant with a saved profile is treated as a bot; the API returns 202 with an empty body and `requestJson` throws "Unexpected end of JSON input" in `submitApplication`. Fix both halves: (a) client — `requestJson` must handle 202/204/empty bodies without throwing, and `submitApplication` must treat 202 exactly like 201 for the user (redirect to /thank-you; the silent drop stays silent); (b) honeypot — rename the field to something no autofill heuristic maps (e.g. `contact_ref_2`), keep `tabIndex=-1`, `aria-hidden`, off-screen positioning, and set `autocomplete="one-time-code"` (the value browsers do not autofill); document in the code why the name is odd. Add a unit test for the empty-body path and a Playwright step that fills the honeypot programmatically and expects the thank-you page. Log as NOTES.md mistake #17 (found by Bhavana in manual testing, not by the suite).

M. **Prospect emails should name the attorney (should-fix, ~10 min).** (a) Confirmation template: add "An attorney will review your application and reach out to you." (b) Status-change template: when `assigned_to` is set, open with "<attorney display name> has reached out regarding your application" (REACHED_OUT) / "…has marked your application as qualified" (QUALIFIED), and set `Reply-To` to that attorney's roster email; unassigned → current wording, no Reply-To. Resolve the name via the same roster lookup the UI uses. Two template tests (assigned / unassigned). No new sends. (c) README Resend section: state that the free tier without a verified domain delivers only to the account owner's address; other recipients are rejected and logged while the lead is kept.

N. **Auto-assign on submit + CC the assigned attorney — MUST-FIX (~15 min, owner requirement).** In `LeadService.create_lead`, when the roster is non-empty: choose the attorney with the fewest open leads (state in PENDING/REACHED_OUT; one GROUP BY query; ties → roster order), set `assigned_to` in the SAME transaction as the insert, write a `lead_events` row (actor `system`, from None → assignee). Emails: prospect confirmation `to=[prospect]`, `cc=[assignee]` (assignee ONLY — never the shared inbox), `Reply-To=assignee`; attorney notification `to=[assignee]`; when the roster is empty, no CC and the notification goes to `ATTORNEY_NOTIFY_EMAIL` as today. `EmailService.send` gains optional `cc` and `reply_to` (console adapter prints them; Resend adapter passes them). Tests: least-loaded pick, tie-break, CC/Reply-To headers on the prospect message, empty-roster fallback, event row written. Update README (auto-assignment paragraph + the free-tier CC caveat) and REQUIREMENTS FR10. "Assign to me" remains for unassigned leads only. Commit proposal: `feat: auto-assign leads on submit and cc the assigned attorney`.

## Part 2 — Release checklist, by actually running commands
1. Fresh clone, for real: `rm -rf /tmp/alma-review && git clone <repo root> /tmp/alma-review && cd /tmp/alma-review && time make setup && make test` — follow README quickstart literally, no knowledge from this repo allowed; paste timing and every step that failed or was ambiguous. Then `git clean -ndx` in the main repo to list anything untracked the app silently depends on.
2. Each row of the deliverables map in PLAN §1: point to the file/route/commit that satisfies it, or mark MISSING.
3. Spec edge cases: required fields enforced server-side? resume required? emails to BOTH prospect and attorney? internal UI unreachable without auth (try curl and browser URL)? state starts PENDING? transition manual only (no auto-transition anywhere)? illegal transitions rejected?
4. Production-repo hygiene: no secrets tracked (`git grep -i 'api_key\|password' -- ':!*.example' ':!docs'`), `.env.example` complete, lint/test/CI present, consistent error handling, no dead code or TODO no-ops.
5. Attribution: every commit has `Agent:`/`Author-mode:`/`Stage:` trailers (`git log --format='%B' | grep -c 'Author-mode'` vs `git rev-list --count HEAD`); `NOTES.md` covers all tracked source files; `docs/AGENT_USAGE.md` ≤ ½ page and cites a real caught mistake.
6. Docs vs code drift: every env var in README exists in `config.py` and vice versa; every endpoint in README exists in the routers.

## Report back
Part 1 scorecard (A–J, PASS/FAIL + evidence), then a findings table `# | severity (blocker/should/nice) | finding | file | suggested fix (1 line)`. Blockers first. Fix blockers immediately (stage by path, propose a `fix:` commit); list the rest for the architect. End with a yes/no: "would I be comfortable submitting this in its current state".

7. Privacy: `git grep -niE "gmail|Users/|/home/|name:" -- . ':!*.lock'` and read `docs/agent/sessions/*` — flag any email, surname, or local path as a blocker (the first name "Bhavana" is the only permitted personal reference).
End with a section **Questions for the architect** (write "none" if none).
