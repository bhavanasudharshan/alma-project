# Prompt 04 — P2: documentation & submission artifacts

> **Role reminder**: you are Claude Code, the implementer and tester. This prompt comes from the planning session (architect / product owner / QA lead), which never writes code. Scope, acceptance criteria and architecture are fixed here; implement them, test them, report back. Put any disagreement under "Questions for the architect" in your report — do not act on it.

Read `CLAUDE.md`, `docs/PLAN.md` §1 (deliverables map) and §4 (P2), and `NOTES.md`. Stage is P2. Documentation only — no code changes unless a doc step exposes a bug (then a separate `fix:` commit, reported).

## Build A — approved P2 features (time-boxed 60 min TOTAL; if the box is hit, ship what is green, revert the rest, record the deferral in NOTES.md)
A1. **Status portal (EXT1)**: `GET /api/v1/leads/track/{code}` — public, rate-limited (SEC1 limiter, 20/min/IP), constant-time lookup by unique `tracking_code`, returns `{state, submitted_at, updated_at, events:[{to_state, at}]}` only — never name/email/resume; 404 generic. Web `/status` page: input for the code, result timeline; link to it from `/thank-you` and in the prospect email. Tests: unknown code → 404 same shape; response has no PII fields.
A2. **Notify hook (EXT2)**: on a transition whose rule has `notify_prospect=True`, schedule a prospect email (template `status_changed`) after commit, same at-most-once semantics as intake emails. Test with `FakeEmailService`.
A3. **Extensibility demo (EXT5)**: add `QUALIFIED` state; `REACHED_OUT → QUALIFIED` transition (notify_prospect=True); badge + "Mark qualified" button; list filter tab. Test the new transition + that PENDING → QUALIFIED is 409. Show in the report exactly which lines changed — this is the E1 proof.
Commit proposals: `feat: public status portal with tracking code`, `feat: prospect notifications on state change + QUALIFIED state`.

## Build B — Playwright (time-boxed 25 min — if it is not green in 25 min, revert it, record the deferral in NOTES.md, and continue)
0. Playwright smoke test in `web/e2e/smoke.spec.ts` against `make dev` (API on SQLite, console email): visit `/apply`, submit a valid lead with a small fixture PDF, expect `/thank-you`; visit `/login`, sign in with env credentials; on `/leads` find the row, click "Mark reached out", expect the state badge to read REACHED_OUT and the button to disappear. Add `make e2e` and a CI job that boots both services and runs it. Commit separately: `test(web): playwright e2e smoke`.

## Build C — one-command bootstrap (~25 min, before the README is finalised)
C1. `scripts/setup.sh` — idempotent, `set -euo pipefail`: check `uv`, `node` (≥20), `pnpm`; print the install hint per tool if missing and exit non-zero; `uv sync` in api/, `pnpm install --frozen-lockfile` in web/; copy `.env.example` → `.env` only if absent; `make migrate`; `--with-e2e` flag installs Playwright browsers; finally print the URLs (web :3000, API docs :8000/api/v1/docs) and where the seeded attorney credentials live (.env). Never print secret values.
C2. `make setup` (runs C1), `make doctor` (prints tool versions, .env presence, DB reachability, chosen adapters — same selection logic as core/deps.py), `make seed` (replace the S0 stub: 4 fictional leads across PENDING / REACHED_OUT / QUALIFIED with tiny generated PDF résumés; idempotent; refuses to run when ENVIRONMENT != local).
C3. `api/requirements.txt` generated via `uv export --format requirements-txt --no-dev -o api/requirements.txt`, first line a comment "GENERATED from uv.lock by make export-reqs — do not edit"; add `make export-reqs`; CI job verifies it is up to date (`uv export … | diff - api/requirements.txt`).
C4. README quickstart becomes exactly: `git clone … && cd alma && make setup && make dev`; prerequisites table (tool · min version · install command macOS/Linux/Windows-WSL); pip fallback paragraph; "If something fails, run `make doctor`".
C5. **Fresh-clone proof (mandatory, timed)**: `git clone <repo root> /tmp/alma-fresh && cd /tmp/alma-fresh && time make setup && make doctor && make test && (make dev & sleep 15; curl -s localhost:8000/api/v1/health; curl -s -o /dev/null -w '%{http_code}' localhost:3000/apply; kill %1)`. Paste the full output and the wall-clock time. Anything that needed a manual step is a README bug — fix it and re-run until the sequence is hands-free. Also add `make verify-clone` that runs exactly this sequence so Bhavana can repeat it on another machine.
Commit proposal: `chore: one-command bootstrap (setup.sh, doctor, seed, requirements export, verify-clone)`.

## Write
1. **`README.md`** (how to run, complete): prerequisites (uv, pnpm/Node 22, optional Docker); 3-command quickstart in no-Docker mode (`cp .env.example .env`, `make migrate`, `make dev`) with the URLs and the seeded attorney credentials pointing at `.env`; full-stack mode with compose profiles and the env changes; configuration table (every setting, default, purpose); API summary table + curl walkthrough; testing/lint/CI; links to `docs/DESIGN.md`, `docs/AGENT_USAGE.md`, `NOTES.md`; a "Demo" section with a placeholder for the Loom link.
2. **`docs/DESIGN.md`** (why/how): requirements restated (functional + non-functional, incl. explicit out-of-scope list); architecture diagram (ASCII or Mermaid) with adapters; decision table in the form `Area | Driver (which requirement forced it) | Choice | Alternative rejected | Price paid`, covering db, file storage, email + timing, auth, state machine, web data-flow (server-side token), repo layout; data model; API contract; state machine diagram; security notes (upload validation, token handling, CORS, secrets); failure modes (email provider down, storage down, duplicate submits) and current behaviour; a "roadmap / extensions" section written from docs/SECURITY_AND_EXTENSIONS.md — EXT3 attorney assignment with the pool + least-loaded + capacity policy and its race note, EXT4 load/trend dashboard query shapes, EXT6 optional intake fields as a worked schema-evolution example (typed enum columns for reason/urgency, validated phone, JSON address, `extra` JSON escape hatch; why all-JSON was rejected), SEC2e ClamAV, SEC4 Turnstile, SEC8 idempotency; "what I would do next for production" (outbox for email, rate limiting/CAPTCHA, virus scan, RBAC, audit log, OpenAPI-generated client, observability). Keep it tight — 2–3 pages, tables over prose.
3. **`docs/AGENT_USAGE.md`** (≤ ½ page): tools used (Cowork/Claude for planning + tracking, Claude Code for implementation, one prompt per stage); what was delegated vs written by hand and why; the one place the agent produced wrong or subtly bad code — take it from `NOTES.md` "Agent mistakes caught" (do NOT invent one; if the table is empty, stop and ask), how it was caught, how it was fixed; pointer to prompts and session transcripts.
4. **`docs/agent/prompts/README.md`**: index of prompts with the commit(s) each produced (`git log --grep 'Stage:'` to map).
5. **`NOTES.md`**: reconcile the file-attribution table against `git ls-files`; every tracked source file must have a row.
6. Commit: `docs: readme, design doc, agent usage writeup` with trailers (`Stage: P2`). Do not push.

## Report back
Anything in the docs you could not verify by running it, and any inconsistency you found between docs and code.
End with a section **Questions for the architect** (write "none" if none).
